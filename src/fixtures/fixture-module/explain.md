### The fixture's explanation

Fixture content. A real module puts the mathematics here: annotated
derivations, displayed equations, and static figures, all typeset at build
time.

$$
\frac{1}{n}\sum_{j=1}^{n} x_j = \frac{s}{n}
$$

The mean decays like $1/n$ for fixed $s$ and does not decay at all once $s$
grows like $n$.
